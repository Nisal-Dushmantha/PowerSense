{
  "name": "backend",
  "version": "1.0.0",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "test": "cross-env NODE_ENV=test jest --runInBand",
    "test:unit": "cross-env NODE_ENV=test jest tests/unit --runInBand",
    "test:integration": "cross-env NODE_ENV=test jest tests/integration --runInBand",
    "test:coverage": "cross-env NODE_ENV=test jest --coverage --runInBand"
    "test": "cross-env NODE_ENV=test JWT_SECRET=test-secret WHATSAPP_BILL_NOTIFICATIONS_ENABLED=false jest --runInBand",
    "test:unit": "cross-env NODE_ENV=test JWT_SECRET=test-secret WHATSAPP_BILL_NOTIFICATIONS_ENABLED=false jest --runInBand tests/unit",
    "test:integration": "cross-env NODE_ENV=test JWT_SECRET=test-secret WHATSAPP_BILL_NOTIFICATIONS_ENABLED=false jest --runInBand tests/integration"
  },
  "author": "",
  "license": "ISC",
  "description": "Backend for PowerSense MERN application",
  "dependencies": {
    "@emailjs/nodejs": "^5.0.2",
    "bcryptjs": "^3.0.3",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "json2csv": "^6.0.0-alpha.2",
    "jsonwebtoken": "^9.0.3",
    "mongoose": "^8.0.0",
    "multer": "^2.0.2",
    "node-cron": "^4.2.1",
    "nodemailer": "^8.0.1",
    "pdfkit": "^0.17.2",
    "qrcode-terminal": "^0.12.0",
    "twilio": "^5.12.2",
    "whatsapp-web.js": "^1.34.6"
  },
  "devDependencies": {
    "cross-env": "^10.1.0",
    "jest": "^30.3.0",
    "mongodb-memory-server": "^11.0.1",
    "nodemon": "^3.0.2",
    "supertest": "^7.2.2"
  }
}
